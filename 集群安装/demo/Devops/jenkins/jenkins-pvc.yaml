kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins
  namespace: devops
spec:
  storageClassName: jenkins-nfs
  resources:
    requests:
      storage: 10Gi
  accessModes:
  - ReadWriteMany

